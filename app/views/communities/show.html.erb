<div class="container">
  <div class="profile">
      <%= form_with model: @community, local: true do |f| %>
      <%= f.text_field :content %>
      <%= f.submit "Create" %>
      <% end %>
  </div>
</div>